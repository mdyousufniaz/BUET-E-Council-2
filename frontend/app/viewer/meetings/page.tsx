// The viewer role's meetings dashboard is the same read-only content as the
// public "/" page (no edit chrome there to begin with) - re-exported here so
// viewers land on a URL that's clearly theirs instead of /workspace/meetings.
export { default } from "../../page";
